extends layout

block content
    .main.container
        .row
            .col-md-8.col-md-offset-2
                h1.display-4.m-b-2 #{user.name}
                p #{user.email}
                p #{user._id}
